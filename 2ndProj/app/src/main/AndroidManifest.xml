<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.passta.a2ndproj">
    <!-- 위치정보에 대한 퍼미션 -->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".start.Dialogue_select_location"
            android:theme="@style/PopUpTheme"/>
        <activity android:name=".start.Dialogue_select_location_2"
            android:theme="@style/PopUpTheme"/>
        <activity
            android:name=".start.Dialogue_add_location"
            android:theme="@style/PopUpTheme" />
        <activity android:name=".start.Page4Activity" />
        <activity android:name=".start.Page3Activity" />
        <activity android:name=".start.Page2Activity" />
        <activity android:name=".MainActivity" /> <!-- <activity android:name=".start.Page1Activity"> -->
        <!-- <intent-filter> -->
        <!-- <action android:name="android.intent.action.MAIN" /> -->
        <!-- <category android:name="android.intent.category.LAUNCHER" /> -->
        <!-- </intent-filter> -->
        <!-- </activity> -->
        <activity android:name=".start.Page1Activity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>